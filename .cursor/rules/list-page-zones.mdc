---
description: i-doms-web 列表页五区划分（A 页头 ~ E 分页、统计卡片、对齐留白）
globs: i-doms-web/**/*.vue
alwaysApply: true
---

# 列表页区域划分

来源：设计规范 v1.3 第 4 章。自上而下 **A–E 五段**，顺序固定；除页头外均为白色卡片。

## 4.1 区域结构

| 区域 | 内容 | 尺寸/说明 |
|------|------|-----------|
| **A 页头** | 面包屑 + 页面标题（+ 描述） | 高 **40~56px**；标题 **20px/600**；描述 **14px text-2** |
| **B 搜索区** | 条件 + 搜索/重置/展开 | 自适应，收起 ≤2 行；见 `list-search-area.mdc` |
| **C 工具栏** | 左业务 / 右视图 | 高 **48px**（padding 8 上下）；见 `list-toolbar.mdc` |
| **D 列表区** | 表格主体 | flex 填充，min **320px**；padding **8px 12px**；见 `list-table-box.mdc` |
| **E 分页区** | 共 N 条 + 每页 + 页码 | 高 **48px**，右对齐；见 `list-pagination.mdc` |

## 4.2 对齐与留白

- 所有卡片左右对齐，卡片间**水平无间隙**
- 页头左对齐；工具栏左业务右视图；分页右对齐
- 超大屏（>1920）内容区可 **1440px 居中**，两侧留白
- 搜索区与列表区间距 **12px**
- 表格列：文字垂直居中、左对齐；**数值列右对齐**

## 4.3 统计卡片（可选）

位于搜索区**上方**，间距 **12px**。

| 项 | 规格 |
|----|------|
| 数量 | 建议 **2~6** 个；超过考虑折叠/轮播/看板 |
| padding | **16px 20px**；每项等分，min-width **160px** |
| 结构 | 指标名（12px text-3）+ 大数值（20px/600）+ 辅助说明（12px text-3） |
| 颜色 | 默认 text-1；金额/数量可用 primary；异常用 warning/danger |
| 交互 | 可点击项 hover pointer + 上浮 1px；点击联动列表过滤 |
| 时间维度 | 周/月/年切换放统计卡片右上角 |

## 子规则引用

搜索 / 工具栏 / 表格 / 分页细则不在此重复，见对应 `.mdc` 文件。
