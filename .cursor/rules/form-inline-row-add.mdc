---
description: i-doms-web 子表行内新增规范（空白行插入、行内搜索选物料、连续录入、空行删除与校验）
globs: i-doms-web/**/*.{vue,js}
alwaysApply: false
---

# 子表行内新增

新增 / 编辑页子表支持「**先插空白行，再在行内搜索选择**」，与顶部「添加物品」弹窗批量选择**互补**。

父级结构见 `form-collapsible-card.mdc`；弹窗选型见 `line-edit-modal.mdc`。

## 两种入口（并存）

| 入口 | 位置 | 行为 |
|------|------|------|
| **+ 添加物品** | 子表工具栏左侧（主按钮） | 打开完整选择弹窗，批量添加 |
| **+ 添加物品行 / + 添加新行** | 表格**最后一行下方**文字按钮 | 插入 **1 条空白行**，进入行内搜索 |

```
[工具栏]  + 添加物品（弹窗） …… 列设置
[表格]
  行1 …
  行N …
[+ 添加物品行]   ← 行内快捷入口
```

参考：`OutboundOrderFormModal.vue`（`addBlankLine`）、`CreatePurchaseRequisitionModal.vue`（弹窗入口）、`BomSubItemMaterialSelect.vue`（行内搜索）

## 空白行

- 插入后**自动获得焦点**
- **首列**（物料编码 / 产品编码）变为可输入搜索框
- 未选中数据前：其他列置灰或显示 **—**

## 行内搜索下拉

| 项 | 约定 |
|----|------|
| 触发 | 输入编码 / 名称关键字 |
| 项展示 | `[编码] 名称`；关键字**高亮** |
| 条数 | 最多 **6~8** 条 |
| 超出 | 底部 **「搜索更多…」** → 打开完整选择弹窗 |

实现参考：`a-auto-complete` + `#dropdownRender`（`BomSubItemMaterialSelect.vue`，`DROPDOWN_LIMIT = 8`）

## 选中回填

选中后自动带出：**规格型号、材质、单位、默认仓库**等只读字段。

随后：

1. 数量、单价等可编辑字段进入编辑态
2. **自动聚焦第一个可编辑单元格**

## 连续录入

支持以下任一（按业务选，可并存）：

- 选中一条后**自动在下方再插**一条空白行
- 在数量单元格按 **Enter** 插入新行
- 便于批量扫码 / 连续搜索录入

## 空行删除

| 行状态 | 删除行为 |
|--------|----------|
| 未填充任何有效数据 | 点行尾删除图标 → **直接移除** |
| 已填充数据 | 删除 → **二次确认** |

## 无结果 / 新建

搜索无匹配时，下拉底部提供入口，避免卡死：

- **「新建物料…」**（若业务允许）
- 或 **「去完整列表选择」** / 「搜索更多…」

## 校验（提交时）

| 规则 | 说明 |
|------|------|
| 空行 | 视为无效：**自动移除**或标红提示 |
| 必填未填 | 该单元格 **红框** + 行首 **红点** |
| 与整单校验 | 同 `form-collapsible-card.mdc`：全量校验、滚到首个错误 |

## 数据流建议

```javascript
function addBlankLine() {
  form.lineItems.push(createBlankLine(/* 默认仓库等 */))
  nextTick(() => focusCodeSearch(form.lineItems.length - 1))
}

function onInlineSelect(row, item) {
  Object.assign(row, mapItemToLine(item)) // 回填只读字段
  nextTick(() => focusFirstEditable(row))
  // 可选：再插空白行
}
```

## 避免

- ❌ 只有弹窗入口、无法行内快速加一行
- ❌ 空白行各列均可乱填（应先选物料再编辑数量等）
- ❌ 下拉无限拉长、无「搜索更多」
- ❌ 空行提交时当有效明细入库
- ❌ 已填数据行删除无确认

## 与弹窗入口的分工

| 场景 | 用 |
|------|-----|
| 一次选多种物料 | 工具栏「添加物品」弹窗 |
| 扫码 / 连续搜一条加一条 | 行内「添加物品行」 |
| 下拉不够用 | 「搜索更多…」回到弹窗 |
