---
description: i-doms-web 图标规范（尺寸三档、描边、颜色、纯图标允许场景）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 图标规范

来源：设计规范 v1.3 第 16 章。

## 尺寸（仅三档）

| 尺寸 | 场景 |
|------|------|
| **16px** | 表格行内操作、列表单元格 |
| **20px** | 工具栏按钮 |
| **24px** | 空态插画、大标题旁 |

禁止 18px、22px 等中间值。

## 描边与颜色

- 线性图标：16/20px → `stroke-width: 1.5`；24px → `2`
- 端点 `linecap: round`；转角 `linejoin: round`
- 颜色 `currentColor`，由 text/功能色令牌驱动
- 功能语义（成功/警告/危险/信息）**必须**用对应功能色，禁止灰图标承载语义

## 纯图标 vs 图标+文字

**允许纯图标**（含义全局一致）：关闭 ✕、刷新 ↻、添加 ＋、展开 ▾、设置 ⚙（若全局统一）。

**必须图标+文字或 title/aria-label**：编辑、更多、导出、筛选等可能歧义的操作。

## 对齐

图标与文字间距 **4–6px**，垂直居中；按钮内图标不换行；同组图标按钮尺寸、色值一致。

工具栏/行内图标按钮须带 **title** 或 **aria-label**（见 `accessibility.mdc`）。
