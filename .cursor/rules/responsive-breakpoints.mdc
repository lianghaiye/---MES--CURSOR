---
description: i-doms-web 响应式断点（页面内边距、搜索列数、表单栅格、弹窗/工具栏降级）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 响应式断点

来源：设计规范 v1.3 第 14 章。与 `list-search-area.mdc`、`drawer-layout.mdc`、`modal-layout.mdc` 配合。

## 断点表

| 断点 | 视口 | 页面内边距 | 搜索条件/行 | 表单栅格 | 弹窗 | 工具栏 |
|------|------|------------|-------------|----------|------|--------|
| 超大屏 | ≥1920px | 24px | 5 个 | 4 列 | 不变 | 全部展开 |
| 常规 | 1280–1919px | 20px | 5 个 | 3 列 | 不变 | 全部展开 |
| 平板横 | 992–1279px | 16px | 3 个 | 2 列 | 缩一档 | 低频入「更多」 |
| 平板竖 | 768–991px | 16px | 2 个 | 2 列 | 520→480 | 收起图标化 |
| 窄屏 | <768px | 12px | 1 个 | 1 列 | 全屏/底部抽屉 | 折叠菜单 |

## 原则

- 断点以下优先保障「**搜索 + 列表**」核心链路，不追求像素级一致
- 「一行 5 个」仅在 **≥1280px** 生效；降级后「两行上限 + 展开/收起」仍成立
- 抽屉窄屏 **100%** 宽（见 `useDrawerWidth`）
- 移动端表格：横向滚动 + 固定操作列；或卡片流（须单独评审）

## 与现有实现

搜索区、抽屉宽度已有 composable/规则落地；新增页面须按上表做 CSS/media 或 composable 降级，勿硬编码单一断点。
