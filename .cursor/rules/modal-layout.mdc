---
description: i-doms-web 弹窗通用规范（尺寸档位、横向表单样式、关闭二次确认、选择器弹窗、表单+多表格弹窗）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 弹窗规范

不需对照列表的居中浮层。需对照列表用抽屉（`drawer-layout.mdc`）；行内明细编辑见 `line-edit-modal.mdc`。

## 尺寸档位

| 档位 | 宽度 | 适用场景 |
|------|------|----------|
| **小** | **400px** | 确认框（删除 / 提交 / 重置二次确认） |
| **中（默认）** | **520px** | ≤8 字段表单、快捷操作、预览 |
| **大** | **720px** | 接近 8 字段、内容较多、带说明图文 |
| **特大 / 全屏** | **90vw × 90vh**（或 1200px） | 极少：批量编辑、大表格选择器 |

```javascript
const MODAL_WIDTH = { s: 400, m: 520, l: 720, xl: '90vw' }
```

## 结构与样式

| 项 | 约定 |
|----|------|
| 遮罩 | `rgba(0,0,0,.45)`，全屏；打开时锁定 body 滚动 |
| 标题 | **16px / 600**，左对齐；确认框可带 ⚠ / ❓ 语义图标 |
| 内容区 | padding **24px**；超高时**仅内容区滚动**，页脚固定；`max-height ≈ 60vh` |
| 页脚 | **右对齐**：取消（次）+ 确定/保存（主），间距 **8px**；按钮高 **32px** |
| 圆角 / 阴影 | **8px** / shadow-2；垂直水平居中 |

```vue
<a-modal
  :open="open"
  :title="title"
  :width="520"
  :mask-closable="false"
  destroy-on-close
  @cancel="handleCancel"
>
  <div class="modal-body">…</div>
  <template #footer>
    <a-button @click="handleCancel">取消</a-button>
    <a-button type="primary" :loading="submitting" @click="handleOk">确定</a-button>
  </template>
</a-modal>
```

## 表单样式（强制）

弹窗内可编辑表单**必须**标签与控件**同一行平行**展示，细则见 `form-horizontal-layout.mdc`、`form-fields.mdc`。

| 项 | 约定 |
|----|------|
| 布局 | `layout="inline"` + class **`horizontal-form`**；**禁止** `layout="vertical"`（label 在上、控件在下） |
| 栅格 | 常规字段 `a-col :span="8"`（一行三列）；中档 520 可用两列 `span="12"`；备注等长文本 `span="24"` + `remark-item` |
| label | 与控件垂直居中；宽约 **88px**；**14px / text-2**；必填红星 |
| 控件 | 统一 **`size="small"`**；高 32px（见 `form-fields.mdc`） |
| 校验 | 失焦/提交校验；失败红框 + 下方 12px 红字；**不关闭**弹窗 |

```vue
<a-form layout="inline" class="horizontal-form" :model="form">
  <a-row :gutter="[12, 8]" style="width: 100%">
    <a-col :span="8">
      <a-form-item label="客户名称" required>
        <a-input v-model:value="form.name" size="small" />
      </a-form-item>
    </a-col>
    <a-col :span="24">
      <a-form-item label="备注" class="remark-item">
        <a-textarea v-model:value="form.remark" :rows="2" size="small" />
      </a-form-item>
    </a-col>
  </a-row>
</a-form>
```

全局样式：`src/styles/horizontal-form.less`（已在 `global.less` 引入）。

确认类弹窗（无表单）不适用本节；行内明细编辑另见 `line-edit-modal.mdc`。

## 关闭与交互（强制）

| 动作 | 无表单（确认类） | 有表单（编辑类） |
|------|------------------|------------------|
| 点击遮罩 | 允许关闭 | **禁止直接关** → 「内容未保存，确认离开？」 |
| ESC | 允许关闭 | 同上 |
| 右上角 ✕ | 允许关闭 | 同上（或仅提交成功 / 取消可关） |
| 取消按钮 | 直接关 | 有改动先二次确认，无改动直接关 |

| 场景 | 行为 |
|------|------|
| **校验失败** | 弹窗**不关闭**；滚到错误字段；**红框 + 红字**（12px） |
| **提交中** | 确定按钮 **loading**，禁用重复点击 |

### 叠加与滚动

- **同时只允许一个「表单弹窗」**打开；确认框可叠加在表单弹窗之上（确认类置顶）
- 打开时锁定 body / 列表滚动；**关闭后恢复滚动位置**
- 有表单弹窗建议 `:mask-closable="false"`，关闭统一走 `handleCancel` 做脏检查

### 确认框文案

| 类型 | 按钮 |
|------|------|
| 普通确认 | 「取消 / 确定」 |
| 危险确认 | 「取消 / **删除**」（主按钮红底 danger）；主按钮文字**必须含动作动词** |

## 选型

| 场景 | 用 |
|------|-----|
| 确认 / ≤8 字段表单 | 本规范小 / 中 / 大弹窗 |
| 行内明细完整编辑 | `line-edit-modal.mdc`（常 720 / 特大） |
| 需看列表对照 | 抽屉 `drawer-layout.mdc` |
| 字段 >12 或极复杂 | 新标签页 `form-page-shell.mdc` |

---

## 选择器弹窗（物料 / 人员 / 多选）

典型：添加产品/物料、选人员、选客户、选供应商。

| 项 | 约定 |
|----|------|
| 尺寸 | **特大**：宽 **90vw**（min **960px**，max **1200px**）；高 **80vh**；内容区固定高、内部滚动 |
| 结构 | 标题 + 顶部搜索 + 主体（左表 + 右已选）+ 底部分页 + 页脚 |
| 顶部搜索 | 1~3 条件 + 搜索/清空；条件多参考 `list-search-area.mdc` |
| 左表 | 主体宽 **70%~75%**；单选/多选；列规则同 `list-table-box.mdc` |
| 右已选 | 宽 **25%~30%**，min **200px**；「已选 N 项」+ 清空；标签卡 + ✕；空态「请从左侧选择」 |
| 分页 | 左表下方右对齐；10/20/50（见 `list-pagination.mdc`） |
| 页脚 | 取消 + **确定 (N)**（显示已选数量） |
| 关闭 | 有已选项时遮罩/ESC/取消 → 「已选择 N 项，确认放弃？」 |

参考：`SelectBomMaterialModal.vue`（结构完整；宽度宜收敛到规范 90vw 档）

```
标题
[搜索条件…] [搜索] [清空]
┌──────────────┬─────────┐
│ 左表 70~75%  │ 已选 N  │
│ checkbox…    │ 标签 ✕  │
│ 分页         │ 清空    │
└──────────────┴─────────┘
              取消｜确定 (3)
```

---

## 表单 + 多表格弹窗（出库 / 发货 / 工单）

典型：发料出库、生产报工、发货确认、订单拆分。

| 项 | 约定 |
|----|------|
| 尺寸 | **特大**：宽 **90vw**（min **960px**，max **1320px**）；高 **90vh**；内容区独立滚动 |
| 结构 | 标题 + 顶部表单 + 提示条（可选）+ 一个或多个表格 + 页脚 |
| 顶部表单 | **2~4 字段/行**，最多 **2 行**；必填红星；备注可整行 |
| 提示条 | 表单与表格之间；info（字 `#1677ff` / 底 `#e6f4ff`）；**13px**；左侧信息图标 |
| 表格区 | 上下排列；每表前区块标题（如「外协产品清单 (2)」）；可含输入/下拉；操作列右对齐 |
| 行内编辑 | 单元格**失焦即校验**；错误红框；滚到首个错误行 |
| 页脚 | 取消 + 确定；确定 **loading** 防重复提交 |

参考：`OutboundOrderFormModal.vue`、`CreateSalesOrderModal.vue`（大表单壳）

## 避免

- ❌ 弹窗表单用 `layout="vertical"`（label 在上、控件在下）
- ❌ 确认框用 720px 过宽
- ❌ 同时打开多个表单弹窗
- ❌ 危险确认主按钮只写「确定」不写动作动词（应用「删除」等）
- ❌ 选择器无右侧已选面板、只能靠表格勾选回忆
- ❌ 校验失败仍关闭弹窗
- ❌ 提交无 loading 导致重复提交
- ❌ 有改动点遮罩直接关无二次确认
- ❌ 表单+多表塞进 520 中档弹窗

## 文档来源

对齐《前端页面设计规范》**第 10 章 弹窗（Modal）**（`前端页面设计规范.html#s10`）。行内明细专章见 `line-edit-modal.mdc`（对应 10.5）。
