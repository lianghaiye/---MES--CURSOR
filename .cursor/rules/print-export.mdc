---
description: i-doms-web 打印与导出（打印样式、文件名、导出范围、异步大数据）
globs: i-doms-web/**/*.{vue,js,ts}
alwaysApply: false
---

# 打印与导出规范

来源：设计规范 v1.3 第 21 章。

## 打印 / PDF

- 顶栏提供「打印 / 导出 PDF」
- 打印样式 `@media print`：**隐藏**搜索区、工具栏、分页、遮罩；仅保留标题 + 表格
- 表格跨页：**每页重复表头**

## 导出文件

| 项 | 规格 |
|----|------|
| 格式 | Excel / CSV（按业务） |
| 文件名 | `模块名_YYYYMMDD_HHmmss.扩展名` |
| 示例 | `库存明细_20260827_143000.xlsx` |

## 导出范围

三选一：**当前页** / **选中项** / **全部**。

无勾选时「选中项」**disabled**。

## 大数据导出

> **1 万行**走异步任务：

1. 提交后返回任务号 + 进度
2. 完成后 Toast / 站内信通知下载
3. 避免前端长时间阻塞

与 `bulk-actions.mdc` 批量导出、工具栏「导出」按钮配合。

## 单据打印版式

- A4 纵向
- 页眉：公司名 + 单号 + 打印时间
- 页脚：页码
- 表格边框 1px；金额列右对齐 + 千分位
