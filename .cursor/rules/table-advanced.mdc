---
description: i-doms-web 表格高级交互（排序、筛选、树表、冻结、吸顶、虚拟滚动、URL 状态）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 表格高级交互

来源：设计规范 v1.3 第 19 章。基础列表见 `list-table-box.mdc`；字号/密度见 `useTableDensity`。

## 排序

- 单击列头：**升序 → 降序 → 无** 循环
- 多列：Shift+单击，表头显示优先级 ① ②
- 当前排序列高亮箭头；服务端排序须展示 loading

## 列内筛选

- 列头筛选图标 → 浮层；多列筛选 **AND**
- 已筛选列头高亮；条件可随列显隐持久化

## 行展开 / 树形

- 每层缩进 **16px**；展开箭头前置
- 懒加载子级：行内 loading
- 父子勾选联动（勾父全选子）

## 冻结列

- 左侧冻结「选择列 / 操作列」（与列显隐联动）
- 横向滚动时固定；**最多 2 列**

## 表头吸顶

纵向滚动：`position: sticky; top: 0`；背景不透明，与内容分离。

## 虚拟滚动

| 行数 | 要求 |
|------|------|
| >200 | **建议**虚拟滚动 |
| >1000 | **必须**虚拟滚动 |
| 虚拟滚动下全选 | 逻辑全选（非 DOM 全选） |

## 行密度

配合字号三档提供 **紧凑 / 标准 / 宽松** 三档行高，默认标准；切换持久化（可与 density 扩展联动）。

## URL 状态（推荐）

排序 / 筛选 / 分页写入 **URL 参数**，支持刷新不丢失、可分享链接。

## 已有实现

- 列宽拖拽 + 持久化、列显隐：`list-table-box.mdc`
- 字号三档：`useTableDensity(pageKey)`
