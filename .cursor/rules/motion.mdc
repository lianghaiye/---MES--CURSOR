---
description: i-doms-web 动效规范（时长档位、缓动曲线、组件进出场、减少动效）
globs: i-doms-web/**/*.{vue,less,css,scss}
alwaysApply: false
---

# 动效规范

来源：设计规范 v1.3 第 18 章。服务于空间关系与状态反馈，**禁止**纯装饰循环动画。

## 时长档位

| 档位 | 时长 | 用途 |
|------|------|------|
| 微 | **100ms** | hover、按压、颜色切换 |
| 短 | **200ms** | 展开/收起、Toast、标签切换 |
| 中 | **250–300ms** | 弹窗/抽屉滑入、下拉浮层 |
| 长 | **400ms+** | 页面转场，尽量少用 |

## 缓动曲线

| 类型 | 曲线 | 用途 |
|------|------|------|
| 标准 | `cubic-bezier(.4,0,.2,1)` | 出现/通用 |
| 减速进入 | `cubic-bezier(0,0,.2,1)` | 抽屉/元素从外进入 |
| 加速退出 | `cubic-bezier(.4,0,1,1)` | 元素退出 |

## 组件进出场

| 组件 | 进入 | 退场 |
|------|------|------|
| Modal | 遮罩淡入 + 面板上滑/缩放 **250ms** | 反向 **200ms** |
| Drawer | 右侧滑入 **250ms** ease-out | 滑出 **200ms** |
| Toast | 顶部淡入 + 下移 **200ms** | 上移淡出 **200ms** |
| 下拉/浮层 | 展开 **150ms** | 收起 **150ms** |
| 折叠面板 | 高度 **200ms** | 高度 **200ms** |

与 Ant Design Vue / 组件库默认动画对齐即可；自定义过渡勿超出上表时长太多。

## prefers-reduced-motion

检测到 `prefers-reduced-motion: reduce` 时，关闭非必要**位移**动画，仅保留透明度变化。

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

## 搜索区展开

展开/收起须有过渡，避免按钮位置漂移（见 `list-search-area.mdc`）。
