---
description: i-doms-web 反馈与状态（Toast、确认、校验、空态、加载、异常）
globs: i-doms-web/**/*.{vue,js,ts}
alwaysApply: false
---

# 反馈与状态规范

来源：设计规范 v1.3 第 13 章。弹窗确认叠加规则见 `modal-layout.mdc` 10.3。

## 13.1 通道选择（强制）

| 通道 | 场景 | 禁止 |
|------|------|------|
| **Toast** | 操作结果，无需决策 | 用弹窗承载成功结果 |
| **Popconfirm** | 行内轻量危险（删一行、停用） | 用 Toast 承载需决策的危险操作 |
| **Modal.confirm** | 批量/不可逆/影响面大 | — |
| **表单内联** | 字段级校验 | — |
| **全局异常页** | 404、无权限、服务不可用 | — |

总原则：**成功静默、失败醒目、危险拦截**。失败/警告须「原因 + 可操作的下一步」。

## 13.2 Toast

| 类型 | 位置 | 时长 | 场景 |
|------|------|------|------|
| 成功 | 顶部居中 | **2s** | 保存、删除、导入完成 |
| 错误 | 顶部居中 | **3s** | 操作失败、接口报错 |
| 警告 | 顶部居中 | **3s** | 库存不足、超预算 |
| 信息 | 顶部居中 | **3s** | 已切换视图、已导出 |

样式：圆角 **8px**；左侧 4px 同色描边或图标；背景为对应功能色浅底；max-width **400px**。

堆叠：向上堆叠，新在上，最多 **3** 条，其余排队。不打断操作；错误/警告可带「查看详情/重试」链接。

### 文案

| 类型 | 合格 | 禁止 |
|------|------|------|
| 成功 | 「保存成功」「已删除 3 条记录」 | 「操作成功」「SUCCESS」 |
| 错误 | 「删除失败：该订单已锁定…」 | 「出错了」（无原因） |
| 警告 | 「当前库存仅剩 5 件…」 | 「注意」（无信息） |

## 13.3 确认

**Popconfirm**：行内删除/停用；气泡 + 取消/确定（确定 danger）；Esc/点外取消；确认后成功 Toast。

**Modal.confirm**：批量删除、提交单据、作废；标题 ⚠ + 说明后果；按钮「取消 / 删除」；遮罩/Esc **允许关闭**（与表单弹窗区分）。

## 13.5 空态

居中图标 + 主文案（**14px text-2**）+ 次文案（**12px text-3**）+ 可选主按钮（**40px**）；容器 min-height **320px**。

示例：「暂无客户数据」+「点击下方按钮创建第一个客户」+「+ 新增客户」。

## 13.6 加载态

| 场景 | 行为 |
|------|------|
| 首次加载 | 表格骨架屏（表头 + 5 行） |
| 翻页/刷新 | 保留旧数据 + 可选顶部细进度条 |
| 搜索中 | 搜索按钮 loading；有数据时不清空，完成后替换 |

## 13.7 异常态

- 接口失败：列表区错误态（图标 + 「加载失败」+ 重试）；有旧数据时仅 Toast
- 404/无权限/503：整页错误页 + 返回按钮 + 简要原因
