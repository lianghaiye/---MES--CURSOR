---
description: i-doms-web 权限驱动显隐（隐藏/置灰/脱敏，前端仅为体验）
globs: i-doms-web/**/*.{vue,js,ts}
alwaysApply: false
---

# 权限驱动显隐

来源：设计规范 v1.3 第 20 章。脱敏见 `watermark-security.mdc`。

## 核心原则

前端显隐仅为**体验**；安全必须以后端校验为准。**隐藏 ≠ 安全**。

## 处理方式

| 场景 | 处理 | 说明 |
|------|------|------|
| 完全无权限 | **隐藏**入口 | 按钮/菜单不渲染，不占位；不可用 disabled 伪装 |
| 有功能但当前数据不可操作 | **置灰** + 原因 | disabled + title（如「单据已锁定」「无该部门权限」） |
| 敏感字段无查看权限 | **脱敏** | 显示 `***`，非留空或隐藏整行 |

## 实现建议

```javascript
// v-if 隐藏无权限入口
v-if="hasPermission('order:delete')"

// 数据态禁用
:disabled="row.locked"
:title="row.locked ? '单据已锁定，无法编辑' : ''"
```

## 禁止

- ❌ 无权限仍渲染可点击按钮（仅 hope 接口拒绝）
- ❌ 敏感字段直接展示完整值并依赖「用户看不到」
- ❌ 前端路由隐藏但 API 无鉴权

接口层须校验权限；越权请求返回 403/业务错误码。
