---
description: i-doms-web 国际化与文本膨胀（留白、省略、数字日期、术语统一）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 国际化与文本膨胀

来源：设计规范 v1.3 第 25 章。

## 布局留白

- 控件/按钮/表头 min-width 预留 **~30%** 膨胀空间
- 德/法等语言较中文长 30–50%；**禁止**定死宽度导致换行溢出

## 长文本

- 省略号 + **title tooltip** 展示全文
- 禁止硬截断导致语义丢失
- 按钮文字与图标**不得重叠**

## 数字 / 日期

精度、去尾零、零值归一、千分位、币种等细则见 **`number-currency-format.mdc`（第 26 章）**。

| 类型 | 规则 |
|------|------|
| 数字 | 跟随 locale 千分位；展示用 `@/utils/numberFormat` |
| 日期 | `YYYY-MM-DD` 或 `DD/MM/YYYY` 按 locale |
| 金额 | 币种符号 + **右对齐**；`¥1,234.5` 格式 |

## 双语对照

主文案 + 次级小字译文，**分两行**，勿混排一行。

## 文案术语（强制）

按钮统一：**确定 / 保存 / 提交 / 取消**，避免同义混用。

禁止中英文夹杂（如「点击 Save」）。

## 与表单/列表联动

- label 宽度用 min-width + flex，非固定像素死锁（表单 88px 为中文基准，i18n 页可适度放宽）
- 表头、工具栏按钮使用 `min-width` + padding，支持文案变长
