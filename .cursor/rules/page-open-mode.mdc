---
description: i-doms-web 页面打开方式（新标签页 vs 弹窗/抽屉判定、保存后刷新、未保存拦截）
globs: i-doms-web/**/*.{vue,js,ts}
alwaysApply: false
---

# 页面打开方式

来源：设计规范 v1.3 第 3 章。与弹窗判定（第 9 章 / `modal-layout.mdc`）配合使用。

## 3.1 默认：新标签页打开（强制）

以下场景**不得**在列表页内以弹窗/抽屉/路由覆盖承载（除非命中弹窗判定规则）：

| 场景 | 打开方式 |
|------|----------|
| 列表 → 新增 | 新标签页 |
| 列表 → 编辑 | 新标签页 |
| 列表 → 详情/查看 | 新标签页 |
| 列表内关联跳转（如单号详情） | 新标签页 |
| 外部/文档链接 | 新标签页 |

实现：`target="_blank"` 或组件库等价 API；链接旁建议 ↗ 图标提示。

**走新标签页的任一理由**：表单字段多需完整滚动；需多条记录对照；编辑中切走不丢内容；需独立收藏/分享/刷新。

## 3.2 新标签页返回逻辑

| 时机 | 行为 |
|------|------|
| 保存成功 | 关闭当前标签（或提示「保存成功，点击返回列表」），**刷新来源列表** |
| 跨标签刷新 | 优先 `BroadcastChannel` / `storage` 事件；不可行时成功页提供「返回列表」+ 刷新标记 |
| 未保存离开 | 关闭标签/刷新/前进后退须触发 `beforeunload` 拦截，防内容丢失 |

## 弹窗/抽屉例外（简要）

不需对照列表、字段 ≤8、无复杂子表 → 弹窗（`modal-layout.mdc`）。需对照列表、分步、子表多 → 抽屉（`drawer-layout.mdc`）或新标签页。
