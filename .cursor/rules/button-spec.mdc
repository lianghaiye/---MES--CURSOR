---
description: i-doms-web 按钮类型、尺寸与组内顺序（7.1–7.2；工具栏布局见 list-toolbar）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 按钮规范

来源：设计规范 v1.3 第 7.1–7.2 节。工具栏布局（7.3）见 `list-toolbar.mdc`。

## 7.1 按钮类型

| 类型 | 样式 | 用途 |
|------|------|------|
| **主按钮** | 填充 primary、白字；hover `#4096ff`；禁用 `#8ec6ff` | 新增、保存、提交、搜索 — **每页最多 1 个** |
| **次按钮** | 白底 + 描边 `#d9d9d9` + text-1 | 重置、导出、取消、返回 |
| **文字按钮** | 无边框，文字 primary | 列表操作列、展开/收起、列设置 |
| **危险按钮** | 红底 `#ff4d4f` 或红字；行内删除红字 + 二次确认 | 删除、停用、清空 |
| **图标按钮** | **32×32**，白底描边；hover 描边 primary | 刷新、字号、列设置、更多 |

## 7.2 尺寸

| 档 | 高度 | 横向 padding | 字号 | 圆角 | 场景 |
|----|------|--------------|------|------|------|
| 大 | 40px | 0 20px | 15px | 8px | 空态主按钮、引导页 |
| **中（默认）** | **32px** | 0 16px | **14px** | **6px** | 页面/工具栏/弹窗页脚 |
| 小 | 24px | 0 10px | 12px | 6px | 表格内紧凑操作 |

## 组内规则

- 按钮组间距 **8px**
- 同一区域：**主按钮靠右**；其余按风险从低到高 / 频率从高到低
- 文案用**动词短语**（新增、编辑、保存），不加句号；危险操作可加图标
- loading 时显示 spinner 并**禁用**，防重复提交

## 禁止

- ❌ 同一视觉区域出现多个主按钮
- ❌ 危险操作无二次确认（见 `feedback-status.mdc`）
- ❌ 纯图标按钮无 title / aria-label（见 `accessibility.mdc`）
