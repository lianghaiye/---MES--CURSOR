---
description: i-doms-web 表单字段规范（label、控件、校验、禁用、区块化）— 布局见 form-horizontal-layout
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 表单规范

来源：设计规范 v1.3 第 12 章。横向布局见 `form-horizontal-layout.mdc`；页壳见 `form-page-shell.mdc`；折叠卡片见 `form-collapsible-card.mdc`。

## 布局

| 项 | 规格 |
|----|------|
| 结构 | label 左对齐 **88px** + 控件 `flex:1` |
| 栅格 | ≥1280：**3 列**；平板 **2 列**；窄屏 **1 列** |
| 行间距 | **16px** |

## Label 与控件

| 项 | 规格 |
|----|------|
| label | **14px**，text-2，垂直居中 |
| 必填 | 红星 `*`（`#ff4d4f`），星号与文字间距 **4px** |
| 控件 | 高 **32px**，圆角 **6px**，边框 `#d9d9d9` |
| focus | 边框 primary + 外发光 `rgba(22,119,255,.1) 2px` |

## 校验（强制）

| 时机 | 行为 |
|------|------|
| blur | 失焦即校验 |
| submit | 全量校验；**失败不关闭**弹窗/页面 |
| 展示 | 控件红边框 + 下方 **12px** 红字（`#ff4d4f`），间距 **4px** |
| 提交失败 | 滚动到**第一个错误**并聚焦；可加顶部汇总 Toast（如「有 3 项填写有误」） |
| 修正后 | 错误提示即时消失 |

文案示例：必填「该项为必填」；格式「请输入正确的手机号」；范围「数量不能超过可用库存 30.5」。

## 提交与禁用

- 提交按钮 **loading**；接口失败用 **Toast** 展示错误概要
- 禁用态：背景 `#f5f5f5`，文字 text-4，边框 `#d9d9d9`

## 区块化（强制）

字段 **>8 个**即分区块：区块标题 **16px/600**，区块间距 **24px**，区块内保持 2~3 列，避免单列表单过长。
