---
description: i-doms-web 批量操作（勾选条、跨页全选、危险确认、异步任务反馈）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 批量操作规范

来源：设计规范 v1.3 第 17 章。危险确认见 `feedback-status.mdc`。

## 触发与位置

- 列表有勾选框即支持批量
- 勾选 **≥1** 行 → 工具栏下方滑出**批量操作条**，直至清空勾选
- 显示「**已选 N 项**」+「清空」

## 全选

| 行为 | 说明 |
|------|------|
| 表头复选框 | 当前页全选 |
| 跨页 | 额外提供「选择全部 X 项（跨页）」 |
| 跨页选中 | 批量操作作用于全部选中；文案「已选全部 X 项」 |

## 按钮与禁用

- 批量条：主操作 + 次要操作
- 无可执行项：**disabled** + title 说明原因
- 危险批量（删除/作废/停用）：**danger** + **Modal.confirm**

## 执行反馈

- 完成 Toast：「已处理 N 项，成功 X / 失败 Y」；失败项可「查看详情」
- 成功后**清空选中**并**刷新列表**

## 大数据 / 耗时

数据量大时走**异步任务**：提交后给任务号 + 进度，完成后再下载/提示，避免前端长时阻塞（见 `print-export.mdc`）。

## 布局参考

```
已选 5 项  [清空]          [批量导出] [批量删除]
```

与 `list-toolbar.mdc` 批量条、勾选后「已选 N 项」提示一致。
