---
description: i-doms-web 无障碍与可访问性（对比度、焦点、键盘、弹窗焦点陷阱）
globs: i-doms-web/**/*.vue
alwaysApply: false
---

# 无障碍与可访问性

来源：设计规范 v1.3 第 15 章。键盘快捷键见 `keyboard-shortcuts.mdc`。

## 对比度

正文/次要文字与背景 **≥ 4.5:1**（WCAG AA）；大字 **≥ 3:1**。

## 焦点

- 所有可交互元素须有可见焦点态：**2px primary 外发光** `rgba(22,119,255,.2)`
- Tab 顺序遵循视觉流；**不得** `outline: none` 且无替代焦点样式

## 键盘交互

| 交互 | 要求 |
|------|------|
| 展开/收起 | Enter 展开 |
| 列设置/浮层 | Esc 关闭 |
| 列宽拖拽 | 可选提供键盘调整降级 |

## 弹窗 / 抽屉

- 打开后焦点**移入并锁定**（焦点陷阱）；关闭后焦点**归还**触发元素
- `role="dialog"` + `aria-label` / `aria-labelledby`
- 图标按钮必须有 **title** 或 **aria-label**；纯图标不得作为唯一语义入口

## 与图标规范联动

见 `icon-spec.mdc`：歧义操作须图标 + 文字或等价 aria 文案。
